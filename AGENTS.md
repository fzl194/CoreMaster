# AGENTS.md

本文件定义本仓库的共享多代理协作工作流。

## 目的

当 Claude Code 和 Codex 共同处理同一个任务时，必须遵循同一套协议，确保计划、实现、审查与查漏补缺保持同步。

## 默认角色分工

- Claude Code：负责任务接收、需求澄清、实现计划、代码修改、执行验证，以及最终交付集成。
- Codex：负责代码审查、风险分析、回归排查、边界情况检查，以及分析文档输出。

如果用户在某个任务里明确重新分配职责，则该任务以用户指令为准；否则默认使用以上分工。

## 协作契约

双方都必须把对方视为真实协作者，而不是被动工具。

- Claude 必须提供足够的实现上下文，便于 Codex 高效审查。
- Codex 必须输出可执行、可落地的审查结果，而不是只有高层评论。
- 除非用户明确要求，否则双方都不应覆盖、回退或丢弃对方的工作。
- 双方都应优先产出可交接的显式材料，而不是依赖隐式上下文。

## 必须遵循的执行流程

对于任何非琐碎任务，使用以下顺序：

1. Claude 创建或更新任务计划。
2. Claude 实现代码改动。
3. Claude 为 Codex 编写交接说明。
4. Codex 审查实现，检查遗漏、回归风险与设计风险。
5. Codex 输出包含结论和建议的分析文档。
6. Claude 根据分析文档修复问题，或明确记录不修复的处置结论。
7. Claude 对用户做最终交付。

如果任务非常小，不值得单独产出分析文档，执行中的代理可以在对话里直接完成交接和审查。对于功能开发、重构、架构调整、插件变更、API 变更、数据结构或表结构变更，默认应产出分析文档。

## 交接接口

Claude 交给 Codex 的材料应尽量简洁，但至少包含：

- 任务摘要
- 范围边界
- 改动文件
- 已执行命令
- 已知假设
- 已知风险或未验证区域
- 需要重点做对抗式审查的问题

推荐结构：

```md
## Claude -> Codex 交接
- 任务：
- 范围：
- 改动文件：
- 已验证内容：
- 假设：
- 风险：
- 审查重点：
```

## 交接文件落盘位置

为了保证双方每次执行时都能互通有无，非琐碎任务必须将交接材料和审查材料落盘，而不是只留在对话里。

### Claude Code 输出位置

Claude 负责把计划、实现说明和交接材料写入 `docs/handoffs/`。

路径约定：

- 计划文档：`docs/plans/YYYY-MM-DD-<task-slug>-impl-plan.md`
- 交接文档：`docs/handoffs/YYYY-MM-DD-<task-slug>-claude-handoff.md`

`claude-handoff` 文档至少包含：

- 任务目标
- 本次实现范围
- 明确不在本次范围内的内容
- 改动文件清单
- 关键设计决策
- 已执行验证
- 未验证项
- 已知风险
- 指定给 Codex 的审查重点

### Codex 输出位置

Codex 负责把审查结论和查漏补缺分析写入 `docs/analysis/`。

路径约定：

- 审查文档：`docs/analysis/YYYY-MM-DD-<task-slug>-codex-review.md`

`codex-review` 文档至少包含：

- 审查背景
- 审查范围
- 发现的问题
- 测试缺口
- 回归风险
- 建议修复项
- 无法确认的残余风险
- 最终评估

### 状态回写规则

- Claude 产出 handoff 后，应在 handoff 文档中标记状态为 `待 Codex 审查`。
- Codex 完成审查后，应在 review 文档中标记状态为 `已审查`，并在 handoff 文档末尾追加 review 文档路径。
- Claude 完成问题处置后，应把 handoff 文档状态更新为 `已处置` 或 `部分处置`，并简要记录处置结论。

Codex 返回的材料应包含：

- 按严重级别排序的问题
- 缺失的测试或验证空白
- 边界情况与回归风险
- 明确的处置建议

推荐结构：

```md
## Codex 审查结论
- 状态：
- 发现的问题：
- 测试缺口：
- 建议修复：
- 剩余风险：
```

## 分析文档规则

Codex 负责将持久化分析文档写入 `docs/analysis/`。

路径约定：

- `docs/analysis/YYYY-MM-DD-<task-slug>-codex-review.md`

文档应至少包含：

1. 背景
2. 审查范围
3. 发现的问题
4. 尚未证明安全的缺口
5. 建议的后续动作
6. 验证说明
7. 最终评估

如果没有发现问题，也必须明确写出“未发现问题”，并记录剩余风险与验证边界。

## 审查标准

Codex 审查时应优先关注：

1. 功能性缺陷
2. 相对于现有行为的回归
3. 相对于计划或需求的不完整实现
4. 数据一致性与迁移风险
5. API 契约不匹配
6. 缺失的校验或错误处理
7. 缺失或薄弱的测试
8. 容易在短期内引发缺陷的可维护性风险

纯样式问题优先级较低，除非它掩盖了正确性或维护性风险。

## 工作规则

- 优先使用小而明确的交付物，而不是依赖隐式上下文。
- 尽可能引用精确的文件和命令。
- 在审查意见被修复或被明确处置前，不应将任务标记为完成。
- 如果 Claude 和 Codex 存在分歧，应在分析文档中记录分歧；当分歧影响正确性、范围或风险时，应升级交由用户决策。

## Git 协作准则

Claude Code 和 Codex 在本仓库内协作时，默认通过 `git` 共享变更上下文、限定审查范围、记录处置过程。

### 基本原则

- 双方都必须在开始工作前查看当前工作树状态。
- 双方都必须避免覆盖、回退或重写对方已存在但未明确要求处理的改动。
- 审查范围默认以本任务新增或修改的 `git diff` 为准，而不是整个仓库。
- 文档交接负责表达结论，`git diff` 负责表达事实改动；两者缺一不可。

### Claude Code 的 Git 职责

Claude 在开始实现前应至少检查：

- `git status --short`
- 必要时查看本任务相关文件的 `git diff`

Claude 在交接给 Codex 时，应在 handoff 文档中补充：

- 当前分支名
- 审查基线
- 建议 Codex 优先查看的 diff 范围
- 本次实现对应的关键提交，若尚未提交则明确写明“基于工作区改动审查”

推荐补充字段：

```md
- 分支：
- 审查基线：
- 关键提交：
- 建议查看的 diff：
```

### Codex 的 Git 职责

Codex 在执行审查时应至少检查：

- `git status --short`
- `git diff`
- 如有需要，查看指定提交的 `git show`

Codex 的 review 文档中应明确说明：

- 本次审查基于哪个分支
- 审查的是工作区 diff、某个提交，还是某两个提交之间的差异
- 是否存在未纳入审查范围但可能影响结论的脏工作区改动

### 提交与处置规则

- 若用户没有明确禁止提交，双方在每次完成一轮有效产出后都应提交并推送到远程仓库，不应只保留本地改动。
- 若用户要求提交，Claude 负责提交实现代码；Codex 默认不替 Claude 代提实现提交，除非用户明确要求。
- Codex 如需为分析文档或补充修复提交内容，应在文档中明确这些改动是否属于“审查产物”还是“修复产物”。
- 任何一方都不得使用会重写历史或丢失改动的危险 `git` 操作，除非用户明确要求。

### Push 规则

- Claude Code 和 Codex 都必须使用 `git push` 将自己的有效产出同步到远程仓库。
- 所谓“有效产出”包括但不限于：实现代码、修复提交、交接文档、审查文档、补充验证结论。
- 如果某一轮工作只完成了本地修改但尚未 `git push`，则该轮工作默认不算完成。
- Claude 在交接给 Codex 前，必须先将当前分支 push 到远程，再在 handoff 文档中写明最新提交哈希。
- Codex 在输出 review 文档或补充修复后，也必须 push 到远程，并在 review 文档中写明对应提交哈希。
- 若因网络、权限或远程策略导致无法 push，必须在文档中明确记录阻塞原因，且不得宣称该轮协作已经闭环。

推荐在文档中增加以下字段：

```md
- 当前分支：
- 最新提交：
- 远程分支：
- Push 状态：
```

### 推荐命令

双方协作时优先使用以下非交互式命令：

- `git status --short`
- `git add -A`
- `git commit -m "<message>"`
- `git push`
- `git diff -- <path>`
- `git diff --stat`
- `git show --stat <commit>`
- `git log --oneline --decorate -n 10`

除非用户明确要求，否则不要依赖交互式 `git rebase`、交互式暂存或其他难以追踪的交互式流程。

## 仓库内的补充约定

- 文档、注释和 UI 文案默认保持中文，除非目标文件本身已经使用英文。
- 后端修改应保持异步 FastAPI + aiosqlite 模型，并维持完整类型标注。
- 前端修改应保持 Vue 3 `script setup`、TypeScript 和现有 Naive UI 结构，除非任务本身要求架构调整。

## 优先级顺序

当不同指令冲突时，按以下顺序处理：

1. 用户直接指令
2. 任务对应的设计文档或实现计划
3. 本 `AGENTS.md`
4. 各代理本地说明或工具说明
