# AGENTS.md

本文件定义本仓库的共享多代理协作工作流。

## 目的

当 Claude Code、Codex 和管理员共同处理同一个项目或任务时，必须遵循同一套协议，确保计划、实现、审查、协调与查漏补缺保持同步。

## 默认角色分工

- 管理员：负责整个项目的运转、任务编排、优先级协调、流程监督，以及协调 Claude Code 和 Codex 的任务分配。管理员可直接提交 git、更新协作文档或补充项目级说明；通常不直接修改业务代码，但若确有修改，也必须像其他协作者一样显式记录。
- Claude Code：负责任务接收、需求澄清、实现计划、代码修改、执行验证，以及最终交付集成。
- Codex：负责代码审查、风险分析、回归排查、边界情况检查，以及分析文档输出。

如果用户在某个任务里明确重新分配职责，则该任务以用户指令为准；否则默认使用以上分工。

## 协作契约

三方都必须把对方视为真实协作者，而不是被动工具。

- 管理员必须尽量显式同步任务背景、职责调整、流程变化、直接文档修改、直接 git 提交，以及任何可能影响协作边界的信息。
- Claude 必须提供足够的实现上下文，便于 Codex 高效审查，也便于管理员理解当前进度和阻塞点。
- Codex 必须输出可执行、可落地的审查结果，而不是只有高层评论，并明确哪些结论受管理员或 Claude 的额外改动影响。
- 除非用户明确要求，否则任何一方都不应覆盖、回退或丢弃另外两方的工作。
- 三方都应优先产出可交接的显式材料，而不是依赖隐式上下文。

## 必须遵循的执行流程

对于任何非琐碎任务，使用以下顺序：

1. 管理员确认任务归属、优先级与协作方式；如无单独说明，默认按本文件分工执行。
2. Claude 创建或更新任务计划。
3. Claude 实现代码改动。
4. Claude 为 Codex 编写交接说明（handoff）。
5. Codex 审查实现，检查遗漏、回归风险与设计风险。
6. Codex 输出包含结论和建议的分析文档（review）。
7. 如果 review 发现需修复的问题，Claude 实现修复并产出独立的修复交付文档（fix），**不得**修改 handoff 或 review 文档。
8. Codex 审查修复，确认闭环后回写 handoff 状态和最终评估。
9. Claude 对用户做最终交付。
10. 如果管理员在任一阶段直接修改文档、补充说明、提交 git 或插入协调决策，相关代理必须在后续 handoff/review/fix 文档中显式记录这些变更对结论的影响。

如果任务非常小，不值得单独产出分析文档，执行中的代理可以在对话里直接完成交接和审查。对于功能开发、重构、架构调整、插件变更、API 变更、数据结构或表结构变更，默认应产出分析文档。

## 交接接口

Claude 交给 Codex 的材料应尽量简洁，但至少包含：

- 任务摘要
- 范围边界
- 改动文件
- 已执行命令
- 已知假设
- 已知风险或未验证区域
- 需要重点做对抗式审查的问题
- 管理员在本轮任务中的介入内容（如有）

推荐结构：

```md
## Claude -> Codex 交接
- 任务：
- 范围：
- 改动文件：
- 已验证内容：
- 假设：
- 风险：
- 管理员介入：
- 审查重点：
```

## 交接文件落盘位置

为了保证三方每次执行时都能互通有无，非琐碎任务必须将交接材料和审查材料落盘，而不是只留在对话里。

### 文件归属

三方必须严格遵守文件归属边界，不得暂存、提交或修改属于对方的文件；管理员若因项目运转需要直接修改文档，应优先修改管理员归属区或新增管理员记录区，不得伪造 Claude 或 Codex 的结论。

| 目录/文件模式 | 归属方 | 说明 |
|---------------|--------|------|
| `docs/analysis/*-codex-review.md` | Codex | Codex 独占，Claude 不得创建、修改或提交 |
| `docs/handoffs/*claude-handoff.md` 初始内容 | Claude | Claude 编写初始交接内容 |
| `docs/handoffs/*claude-handoff.md` 状态/回写区域 | Codex | Codex 回写审查结论和状态标记 |
| `docs/handoffs/*claude-fix.md` | Claude | Claude 修复代码后的独立交付文档 |
| `docs/plans/*-impl-plan.md` | Claude | Claude 的实现计划 |
| `docs/admin/*` | 管理员 | 管理员的项目级协调、任务分派、状态公告、补充说明 |
| 源代码（`backend/`、`frontend/`） | Claude | Claude 负责实现和修改；若管理员直接修改，必须在相关文档中记录 |

### Claude Code 输出位置

Claude 负责把计划、实现说明和交接材料写入 `docs/handoffs/`。

路径约定：

- 计划文档：`docs/plans/YYYY-MM-DD-<task-slug>-impl-plan.md`
- 交接文档：`docs/handoffs/YYYY-MM-DD-<task-slug>-claude-handoff.md`
- 修复文档：`docs/handoffs/YYYY-MM-DD-<task-slug>-claude-fix.md`

`claude-handoff` 文档至少包含：

- 任务目标
- 本次实现范围
- 明确不在本次范围内的内容
- 改动文件清单
- 关键设计决策
- 已执行验证
- 未验证项
- 已知风险
- 指定给 Codex 的审查重点
- 管理员本轮直接介入记录（如有）

### Codex 输出位置

Codex 负责把审查结论和查漏补缺分析写入 `docs/analysis/`。

路径约定：

- 审查文档：`docs/analysis/YYYY-MM-DD-<task-slug>-codex-review.md`

`codex-review` 文档至少包含：

- 审查背景
- 审查范围
- 发现的问题
- 测试缺口
- 回归风险
- 建议修复项
- 无法确认的残余风险
- 管理员介入对审查范围或结论的影响
- 最终评估

### 管理员输出位置

管理员负责把项目级协调、任务分派、跨代理约束、流程更新和直接操作记录写入 `docs/admin/`。

路径约定：

- 协调文档：`docs/admin/YYYY-MM-DD-<topic-slug>-admin-note.md`

`admin-note` 文档建议包含：

- 背景
- 决策事项
- 影响范围
- 相关任务或文档
- 直接执行的操作
- 需要 Claude 或 Codex 跟进的事项

### 状态回写规则

- Claude 产出 handoff 后，应在 handoff 文档中标记状态为 `待 Codex 审查`。
- Codex 完成审查后，应在 review 文档中标记状态为 `已审查`，并在 handoff 文档末尾追加 review 文档路径和审查结论。
- Claude 根据 review 完成代码修复后，应产出独立的修复交付文档（如 `*claude-fix.md`），**不得**自行回写 handoff 状态或 review 结论。
- Codex 确认修复闭环后，由 Codex 负责更新 handoff 文档状态为 `已处置` 或 `部分处置`，并回写最终评估。
- 管理员如在流程中插入了协调决策、额外提交或文档调整，不直接回写 Claude/Codex 专属结论区域；如需补充状态，应通过 `docs/admin/` 文档或 handoff/review 预留的“管理员介入记录”字段说明。

Codex 返回的材料应包含：

- 按严重级别排序的问题
- 缺失的测试或验证空白
- 边界情况与回归风险
- 明确的处置建议
- 管理员介入是否改变处置优先级或验证边界

推荐结构：

```md
## Codex 审查结论
- 状态：
- 发现的问题：
- 测试缺口：
- 建议修复：
- 管理员影响：
- 剩余风险：
```

## 分析文档规则

Codex 负责将持久化分析文档写入 `docs/analysis/`。

路径约定：

- `docs/analysis/YYYY-MM-DD-<task-slug>-codex-review.md`

文档应至少包含：

1. 背景
2. 审查范围
3. 发现的问题
4. 尚未证明安全的缺口
5. 建议的后续动作
6. 验证说明
7. 管理员介入说明
8. 最终评估

如果没有发现问题，也必须明确写出“未发现问题”，并记录剩余风险与验证边界。

## 审查标准

Codex 审查时应优先关注：

1. 功能性缺陷
2. 相对于现有行为的回归
3. 相对于计划或需求的不完整实现
4. 数据一致性与迁移风险
5. API 契约不匹配
6. 缺失的校验或错误处理
7. 缺失或薄弱的测试
8. 容易在短期内引发缺陷的可维护性风险
9. 管理员直接操作是否导致审查范围漂移、文档失配或结论失效

纯样式问题优先级较低，除非它掩盖了正确性或维护性风险。

## 工作规则

- 优先使用小而明确的交付物，而不是依赖隐式上下文。
- 尽可能引用精确的文件和命令。
- 在审查意见被修复或被明确处置前，不应将任务标记为完成。
- 如果 Claude、Codex 和管理员之间存在分歧，应在分析文档中记录分歧；当分歧影响正确性、范围或风险时，应升级交由用户决策。
- 管理员直接修改文件或直接提交后，Claude 和 Codex 在继续工作前都应重新检查相关 diff，确认这些改动是否需要纳入当前结论。

## Git 协作准则

Claude Code、Codex 和管理员在本仓库内协作时，默认通过 `git` 共享变更上下文、限定审查范围、记录处置过程。

### 基本原则

- 三方都必须在开始工作前查看当前工作树状态。
- 三方都必须避免覆盖、回退或重写另外两方已存在但未明确要求处理的改动。
- 审查范围默认以本任务新增或修改的 `git diff` 为准，而不是整个仓库。
- 文档交接负责表达结论，`git diff` 负责表达事实改动；两者缺一不可。
- 管理员的直接提交或直接修改，同样属于协作上下文的一部分，不能被视为“仓库外噪音”。

### Claude Code 的 Git 职责

Claude 在开始实现前应至少检查：

- `git status --short`
- 必要时查看本任务相关文件的 `git diff`

Claude 在交接给 Codex 时，应在 handoff 文档中补充：

- 当前分支名
- 审查基线
- 建议 Codex 优先查看的 diff 范围
- 本次实现对应的关键提交，若尚未提交则明确写明“基于工作区改动审查”
- 管理员在当前分支上的相关提交或工作区改动（如有）

推荐补充字段：

```md
- 分支：
- 审查基线：
- 关键提交：
- 建议查看的 diff：
- 管理员相关改动：
```

### Codex 的 Git 职责

Codex 在执行审查时应至少检查：

- `git status --short`
- `git diff`
- 如有需要，查看指定提交的 `git show`

Codex 的 review 文档中应明确说明：

- 本次审查基于哪个分支
- 审查的是工作区 diff、某个提交，还是某两个提交之间的差异
- 是否存在未纳入审查范围但可能影响结论的脏工作区改动
- 是否存在管理员提交或管理员文档修改影响了本次审查基线

### 管理员的 Git 职责

管理员在直接操作仓库前后应尽量检查：

- `git status --short`
- 必要时查看 `git diff --stat`
- 必要时记录 `git log --oneline --decorate -n 10`

管理员若执行了以下动作，应在 `docs/admin/` 或相关任务文档中显式记录：

- 直接 `git commit`
- 直接 `git push`
- 直接修改协作文档
- 直接修改非协作文档但会影响任务范围、验证边界或评审结论

推荐补充字段：

```md
- 当前分支：
- 直接操作：
- 相关提交：
- 影响任务：
- 已通知对象：
```

### 提交与处置规则

- 若用户没有明确禁止提交，三方在每次完成一轮有效产出后都应提交并推送到远程仓库，不应只保留本地改动。
- 若用户要求提交，Claude 负责提交实现代码；Codex 默认不替 Claude 代提实现提交，除非用户明确要求。
- Codex 如需为分析文档或补充修复提交内容，应在文档中明确这些改动是否属于“审查产物”还是“修复产物”。
- 管理员如需提交协作文档、项目管理文档或协调性修复，应明确标记为“管理员产物”，避免与 Claude 或 Codex 的责任边界混淆。
- 任何一方都不得使用会重写历史或丢失改动的危险 `git` 操作，除非用户明确要求。

### Push 规则

- Claude Code、Codex 和管理员都必须使用 `git push` 将自己的有效产出同步到远程仓库。
- 所谓“有效产出”包括但不限于：实现代码、修复提交、交接文档、审查文档、补充验证结论、管理员协调文档、管理员直接提交记录。
- 如果某一轮工作只完成了本地修改但尚未 `git push`，则该轮工作默认不算完成。
- Claude 在交接给 Codex 前，必须先将当前分支 push 到远程，再在 handoff 文档中写明最新提交哈希。
- Codex 在输出 review 文档或补充修复后，也必须 push 到远程，并在 review 文档中写明对应提交哈希。
- 管理员若插入了有效产出，也必须 push 到远程，并在相应文档中写明对应提交哈希及影响范围。
- 若因网络、权限或远程策略导致无法 push，必须在文档中明确记录阻塞原因，且不得宣称该轮协作已经闭环。

推荐在文档中增加以下字段：

```md
- 当前分支：
- 最新提交：
- 远程分支：
- Push 状态：
```

### 推荐命令

三方协作时优先使用以下非交互式命令：

- `git status --short`
- `git add <具体文件路径>`（**禁止**使用 `git add -A` 或 `git add .`，必须逐文件暂存，避免误提交对方的文件）
- `git commit -m "<message>"`
- `git push`
- `git diff -- <path>`
- `git diff --stat`
- `git show --stat <commit>`
- `git log --oneline --decorate -n 10`

除非用户明确要求，否则不要依赖交互式 `git rebase`、交互式暂存或其他难以追踪的交互式流程。

## 仓库内的补充约定

- 文档、注释和 UI 文案默认保持中文，除非目标文件本身已经使用英文。
- 后端修改应保持异步 FastAPI + aiosqlite 模型，并维持完整类型标注。
- 前端修改应保持 Vue 3 `script setup`、TypeScript 和现有 Naive UI 结构，除非任务本身要求架构调整。
- 管理员通常不直接修改代码；如确需修改，应在相关任务文档中说明原因、范围、影响和后续责任归属。

## 轻量协作管理机制

随着任务和需求增多，若每一轮沟通都落成独立文档，后续会迅速失控。为兼顾“可追踪”和“可管理”，仓库内增加一套**双层协作机制**：

### 一、正式文档层：只保留里程碑产物

以下文档仍然保留，且只用于沉淀阶段性正式结论，不用于日常来回留言：

- `docs/plans/*-impl-plan.md`
- `docs/handoffs/*claude-handoff.md`
- `docs/handoffs/*claude-fix.md`
- `docs/analysis/*-codex-review.md`
- `docs/admin/*-admin-note.md`

使用原则：

- 一个任务通常只保留一套主计划、主交接、主审查、主修复文档。
- 小修订优先直接更新原文档，不为每次往返都新增一份新文档。
- 只有当任务进入新的明确阶段（如修复轮次、正式复审）时，才新增新文档。

### 二、协作对话层：统一进入根目录消息文件

除正式文档外，Claude Code、Codex 与管理员之间的日常同步、提问、阻塞说明、补充上下文、短反馈，统一写入根目录消息文件，而不是继续新增零散 markdown 文档。

#### 1. 任务索引：`COLLAB_TASKS.md`

用途：

- 作为当前协作任务的总入口
- 记录每个任务的唯一 ID、当前状态、负责人、关联正式文档路径
- 明确“这个任务现在看哪几个文件是准的”

规则：

- 每个非琐碎任务只创建一条任务记录
- 后续所有 plan / handoff / review / fix / admin-note 都挂到这条记录下
- 状态变化优先更新这里，而不是在多个文档头部重复写不同版本的状态

推荐字段：

```md
## <task-id>
- 标题：
- 状态：
- 当前阶段：
- Claude：
- Codex：
- 管理员：
- 计划文档：
- 交接文档：
- 审查文档：
- 修复文档：
- 管理员文档：
- 最新消息序号：
- 备注：
```

#### 2. 代理留言板：`AGENT_MESSAGES.md`

用途：

- 作为 Claude、Codex、管理员之间的“仓库内对话记录”
- 记录短消息，而不是正式结论
- 用于替代“为了说一句话就新建一个 md”

适合写入的内容：

- Claude 给 Codex 的审查提示
- Codex 给 Claude 的补充问题或修复建议
- 管理员插入的范围调整、优先级变化、额外说明
- 阻塞、疑点、假设变化、待确认项

不适合写入的内容：

- 完整正式审查结论
- 完整实现计划
- 最终交付说明

消息格式：

```md
## MSG-0001
- 任务：<task-id>
- 时间：YYYY-MM-DD HH:MM
- From：Claude | Codex | 管理员
- To：Claude | Codex | 管理员 | All
- 类型：handoff-note | review-note | blocker | question | decision | status
- 关联文件：
- 内容：
- 预期动作：
```

管理规则：

- `AGENT_MESSAGES.md` 采用**追加写入**，不回改历史消息正文；如需修正，新增一条 follow-up。
- 每条消息必须关联 `task-id`。
- 长消息应先总结为 3-10 行短说明，再附相关正式文档路径。
- 若某条消息已经形成正式结论，应在后续消息中标记“已沉淀到哪个正式文档”，避免留言板变成第二套文档系统。

### 三、推荐协作节奏

对于一个正常任务，推荐按以下方式协作：

1. 在 `COLLAB_TASKS.md` 新增任务记录，定义唯一 `task-id`
2. Claude 产出正式 `plan`
3. Claude 实现代码后产出正式 `handoff`
4. Claude 若有额外提醒或对抗点，写入 `AGENT_MESSAGES.md`
5. Codex 读取 `handoff` + `AGENT_MESSAGES.md` 后产出正式 `review`
6. 若发现问题，Codex 可先在 `AGENT_MESSAGES.md` 留短消息，再由 Claude 产出正式 `fix`
7. 过程中任务状态和“当前应看的文档”统一更新到 `COLLAB_TASKS.md`

### 四、控制文档膨胀的硬规则

- 短沟通写 `AGENT_MESSAGES.md`，不要新建文档。
- 任务导航写 `COLLAB_TASKS.md`，不要靠人脑记忆当前有效文件。
- 正式结论只写入 plan / handoff / review / fix / admin-note。
- 同一任务若没有进入新阶段，不新增新文档，只更新原文档和索引。
- 若消息文件过长，可按日期或季度归档为 `AGENT_MESSAGES.archive-YYYYQn.md`，但 `COLLAB_TASKS.md` 中必须保留最新活跃任务入口。

## 优先级顺序

当不同指令冲突时，按以下顺序处理：

1. 用户直接指令
2. 任务对应的设计文档或实现计划
3. 本 `AGENTS.md`
4. 各代理本地说明或工具说明
